{
  "name": "db",
  "private": true,
  "version": "0.1.0",
  "main": "src/client.ts",
  "types": "src/client.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.16.0"
  },
  "devDependencies": {
    "prisma": "^5.16.0"
  }
}
